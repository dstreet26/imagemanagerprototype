<h2>Edit Project</h2>

<%= render "form.html", Map.put(assigns, :action, image_assets_project_path(@conn, :update, @project)) %>

<span><%= link "Back", to: image_assets_project_path(@conn, :index) %></span>
